import CollectionException from "./CollectionException";

/**
 * An exception class that can be thrown when an invalid operator has been requested.
 */
export default class InvalidOperatorException extends CollectionException {
}
